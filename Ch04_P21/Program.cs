using System;                       // Console 등 기본 기능 제공한다.
using System.Collections.Generic;   // 제네릭 컬렉션(List, Dictionary 등)
using System.Linq;                  // 데이터 쿼리 기능
using System.Text;                  // 문자열 처리 기능
using System.Threading.Tasks;       // 비동기 작업 기능

namespace Ch04_P21                  // 이 코드가 속한 네임스페이스 (가장 큰 코드 그룹화 단위이다.)
{
    internal class Program          // Program 클래스 (메서드들을 묶는 코드 단위)
    {
        static void Main(string[] args)  // Main 메서드 (프로그램의 시작점, 여기서부터 실행됨)
        {
            Console.WriteLine("Hello, World!");  // "Hello, World!" 문자열 출력
        }   // Main 메서드 끝
    }   // Program 클래스 끝
}   // Ch04_P21 네임스페이스 끝